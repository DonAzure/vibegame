# 🎙️ 말로 하는 슈팅 게임 (VibeGame HTML5) 기획서

## 1. 프로젝트 개요 (Overview)

- **프로젝트명**: 말로 하는 슈팅 게임 (VibeGame HTML5)
- **플랫폼**: 모든 모바일 스마트폰(iOS Safari, Android Chrome, Edge 등) 및 PC 브라우저 (오프라인 로컬 환경 완벽 지원)
- **주요 기술 스택**: HTML5 Canvas, Vanilla JavaScript, CSS3, Web Speech API (`SpeechRecognition`), Web Audio API Synthesizer
- **개발 목적**: 기존 안드로이드 전용 앱 프로젝트를 모든 기기에서 구동 가능한 HTML5 웹 게임으로 전면 전환하고, 음성 인식 지연에 따른 조작 난이도와 탄막/외계인 간 열(Lane) 불일치로 인한 사격 타격감 문제를 완벽히 개선함.

---

## 2. 핵심 개선 및 퀄리티 업그레이드 내역 (Recent Updates)

### 🎨 1) 고품질 2D 그래픽 에셋 전면 교체
- 기존의 단순한 도형(세모, 원)을 대체하여 고품질의 2D 이미지 에셋을 로컬에서 직접 렌더링합니다.
- **플레이어 (전투기)**: 네온 푸른빛이 흐르는 사이버펑크 2D 전투기 (`hero_spaceship.png`)
- **운석 (기본 장애물)**: 리얼한 3D 입체 우주 암석 (`asteroid_obstacle.png`)
- **외계인 보스 (UFO)**: 붉은 오라를 뿜는 우주 함선 (`ufo_boss.png`)
- 캐시 방지(Cache-busting) 쿼리를 제거하여 로컬 환경(`file:///`)에서 더블클릭만으로도 아무 에러 없이 즉시 그래픽을 로드할 수 있도록 최적화되었습니다.

### 🎯 2) 5-Lane (열/라인) 정렬 시스템 (명중률 100% 보장)
- 화면 전체 너비를 **5개의 정밀 라인(Lane 0 ~ Lane 4)**으로 분할.
- 플레이어 비행기, 외계인, 운석, 미사일이 모두 동일한 라인 열 축을 중심으로 이동.
- 좌/우 음성 명령 시 딱 1 라인씩 정확히 이동하며, 플레이어가 위치한 라인 중심에서 직진 미사일이 발사되어 **동일 열의 적을 100% 명중**시킴.

### 🧠 3) 정밀한 1대1 음성 인식 매핑 (중복 이동 버그 해결)
- 구글 음성 인식기(`SpeechRecognition`)의 지속적인 부분 인식(Interim Results)으로 인해 한 번 말할 때 2~3칸씩 연속 이동하던 고질적인 버그를 완벽 해결했습니다.
- 각 음성 문장의 고유 번호(`resultIndex`)와 문자열 인덱스를 정밀 추적하여, 말이 길어지더라도 **같은 단어에는 무조건 1회만 이동하도록 로직을 재설계**했습니다. (튜토리얼 및 본 게임 모두 적용)

### ⚙️ 4) 사용자 친화적 UI 및 키보드/터치 동기화
- 글자가 하단 버튼에 가려지는 문제를 해결하고, 화면 우상단 및 터치 패드 옆에 **항상 떠있는 `⚙️ 설정` 버튼**을 배치하여 언제든 단어를 바꿀 수 있게 개선했습니다.
- 키보드(좌우 방향키)를 꾹 누르고 있을 때 여러 칸을 미끄러지듯 이동하던 현상(`e.repeat`)을 차단하여 무조건 1회 1칸 이동을 보장합니다.
- **튜토리얼 모드**에서도 음성뿐만 아니라 키보드/터치 버튼만으로도 원활하게 튜토리얼을 훈련하고 통과할 수 있도록 컨트롤이 동기화되었습니다.
- 거슬리는 붉은색 권한 알림창 등 불필요한 UI를 영구 제거하여 시각적 몰입도를 높였습니다.

---

## 3. 게임 규칙 및 진행 구조 (Game Logic & Flow)

1. **시작 화면 (Boot Overlay)**
   - 게임 설명 및 난이도(`쉬움`/`보통`/`어려움`) 선택.
   - **🚀 시작하기** 버튼 클릭 시 마이크 권한 요청 및 Web Audio 초기화.

2. **튜토리얼 모드 (Tutorial)**
   - 기본 음성 명령어인 `"왼쪽"`, `"오른쪽"`, `"뿅"` 연습.
   - 음성, 키보드, 터치 모두 지원.

3. **메인 플레이 (Stage 1 ~ 5)**
   - **운석(Asteroid)**: 기본 적 (HP 1, 격추 시 +15점)
   - **UFO 외계인**: Stage 3부터 출현 (HP 2~3, 좌우 흔들림 및 탄막 투하, 격추 시 +40점)
   - 시간 경과에 따라 Stage 1에서 Stage 5까지 단계별 진입.

4. **설정 메뉴 (Settings)**
   - 플레이 중 `"설정"` 음성 명령어나 톱니바퀴 버튼(⚙️)으로 진입.
   - 이동(좌), 이동(우), 발사 명령어를 원하는 단어로 커스텀 변경 가능.

5. **게임 오버 및 리더보드 (Game Over & Leaderboard)**
   - 체력(HP) 0 도달 시 게임 종료.
   - 음성으로 이름을 말하여 점수 저장 (`localStorage` 기반 Top 8 랭킹).

---

## 4. 디렉터리 구조 (Directory Structure)

`C:\Users\Westender\OneDrive\MyGame\말로하는슈팅게임\`
├── `play_game.html`       # 완벽하게 최적화된 게임 실행용 메인 HTML 파일 (더블클릭 실행 지원)
├── `index.html`           # play_game.html과 동일한 백업/기본 인덱스 파일
├── `audio.js`             # Web Audio API 레트로 효과음 합성 모듈
├── `hero_spaceship.png`   # 플레이어 전투기 그래픽
├── `asteroid_obstacle.png`# 운석 그래픽
├── `ufo_boss.png`         # 외계인 보스 그래픽
└── `게임기획서.md`         # 프로젝트 구현 및 기능 기획 문서 (본 문서)

---

## 5. 실행 가이드 (How to Run)

가장 직관적이고 쉬운 실행 방법을 지원합니다.

1. 내 컴퓨터 폴더에서 **`play_game.html`** 또는 **`index.html`** 파일을 찾아 **직접 더블클릭**합니다.
2. 브라우저가 열리면 화면의 **시작하기** 버튼을 누릅니다.
3. 마이크 권한 요청 팝업이 뜨면 **"허용"**을 누릅니다. (로컬 환경 특성상 허용 창이 자주 뜰 수 있으나, 정상 작동합니다.)
4. 마이크에 **"왼쪽"**, **"오른쪽"**, **"뿅"**(발사)이라고 말하거나 하단 터치 버튼, 키보드 방향키를 사용하여 게임을 즐깁니다.
